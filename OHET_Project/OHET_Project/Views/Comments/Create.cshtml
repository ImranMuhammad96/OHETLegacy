@model OHET_Project.Models.models.Comment

@{
    ViewBag.Title = "Create";
}

<section id="three" class="wrapper style2">
    <div class="inner">
        <div class="inner">
            <header class="align-center">
                <p class="special"></p>
                <h2>Create comment</h2>
            </header>
        </div>
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="12u$">
                @Html.TextAreaFor(model => model.Description, 6, 1, htmlAttributes: new { @class = "form-control", @placeholder = "Description" })
                @Html.ValidationMessageFor(model => model.Description, "", new { @class = "text-danger" })
            </div>

            <br />

            <footer class="align-center">
                <button type="submit" class="button alt">Create</button>
            </footer>
        }
    </div>
    <div class="align-center">
        @Html.ActionLink("Back to Article", "Details", "Posts", new { id = ViewBag.IDPost }, new { })
    </div>
</section>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
