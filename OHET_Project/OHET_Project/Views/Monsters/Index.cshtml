@model IEnumerable<OHET_Project.Models.models.Monster>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@using (Html.BeginForm())
{
    <p>
        Find by name: @Html.TextBox("SearchString")
        <input type="submit" value="Search" class="btn btn-default" />
        @Html.ActionLink("Clear", "Index", new { isOff = ViewBag.isOff })
    </p>
}

<table class="table">
    @*
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.conceptLvl)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th></th>
    </tr>
    *@
@foreach (var item in Model.OrderBy(x => x.name).OrderBy(y => y.conceptLvl)) {
    if (@item.Content.ApplicationUserId == ViewBag.userId || @item.Content.isOfficial == true)
    {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.conceptLvl)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        @if (@item.description.Split().ToArray().Length <= 20)
        {
            <td>
                @item.description
            </td>
        }
        else
        {
            <td>
                @String.Join(" ", @item.description.Split().Take(20))
                @Html.ActionLink("...", "Details", new { id = item.IDMonster })
            </td>
        }
        <td>
            @Html.ActionLink("Details", "Details", new { id = item.IDMonster })
        </td>
        @if (@item.Content.ApplicationUserId == ViewBag.userId || User.IsInRole("Admin"))
        {
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.IDMonster }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.IDMonster })
            </td>
        }
    </tr>
    }
}
</table>
