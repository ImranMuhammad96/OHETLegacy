@model OHET_Project.Models.models.Post

@{
    ViewBag.Title = "Delete";
}

<section id="three" class="wrapper style2">
    <div class="inner">
        <div class="inner">
            <header class="align-center">
                <p class="special">Are you sure you want to delete this article?</p>
                <h2>Delete Article</h2>
            </header>
        </div>
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="12u$">
                Title
                @Html.EditorFor(model => model.Title, new { htmlAttributes = new { @class = "form-control", disabled = "disabled" } })
            </div>
            <br />
            <div class="12u$">
                Description
                @Html.TextAreaFor(model => model.Description, new { @class = "form-control", disabled = "disabled", @rows = 24 })
            </div>
            <br />
            <div class="12u$">
                Date
                <input type="text" value="@Model.Date" disabled="disabled" />
            </div>

            <br />

            <footer class="align-center">
                <input type="submit" value="Delete" class="btn btn-default" />
            </footer>
        }
        <div class="align-center">
            @Html.ActionLink("Back to List", "Index")
        </div>
    </div>
</section>
